version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
    node-tests:
        jobs:
            build:
                docker:
                    - image: circleci/node
                steps:
                    - chockout
                    - run: npm install --save
                    - run: npm test